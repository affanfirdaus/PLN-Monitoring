<style>
    :root {
        --primary-blue: #0099ff;
        --pln-blue: #2F5AA8;
    }
    /* Topbar Styling */
    .fi-topbar {
        background-color: white;
        border-bottom: 2px solid var(--primary-blue);
    }
    
    /* Modify sidebar brand area to look like reference if possible */
    .fi-sidebar-header {
        background-color: #f8fafc;
        border-bottom: 1px solid #e2e8f0;
    }
    
    /* Make the brand text larger/bolder */
    .fi-sidebar-header .fi-logo {
        font-size: 1rem;
        font-weight: 700;
        color: var(--pln-blue);
    }
</style>
